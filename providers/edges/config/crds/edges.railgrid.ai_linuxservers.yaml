---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: linuxservers.edges.railgrid.ai
spec:
  group: edges.railgrid.ai
  names:
    kind: LinuxServer
    listKind: LinuxServerList
    plural: linuxservers
    shortNames:
    - ls
    singular: linuxserver
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.connected
      name: Connected
      type: boolean
    - jsonPath: .status.lastHeartbeatTime
      name: Last Heartbeat
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    - jsonPath: .status.agentVersion
      name: Agent Version
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: "LinuxServer is a managed bare-metal/VM Linux host reachable
          through the hub\nvia an outbound reverse tunnel from its agent, accessed
          over SSH.\n\nAgents register via the provider's agent-ingress endpoint:\n\n\t/services/providers/edges/agent/{cluster}/apis/edges.railgrid.ai/v1alpha1/linuxservers/{name}/proxy\n\nUsers
          access it via the ssh subresource:\n\n\t/services/providers/edges/edgeproxy/clusters/{cluster}/apis/edges.railgrid.ai/v1alpha1/linuxservers/{name}/ssh"
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: LinuxServerSpec defines the desired state of a LinuxServer.
            properties:
              sshCredentialsRef:
                description: SSHCredentialsRef references a Secret with admin-configured
                  SSH credentials.
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              sshHostKey:
                description: |-
                  SSHHostKey pins the sshd host public key (authorized_keys format, e.g.
                  "ssh-ed25519 AAAA...") the provider verifies SSH sessions against. When
                  set it takes precedence over the agent-reported status.sshHostKey, which
                  a compromised agent could otherwise assert.
                type: string
              sshHostKeyPolicy:
                default: strict
                description: |-
                  SSHHostKeyPolicy controls what happens when no host key is known (neither
                  spec.sshHostKey nor status.sshHostKey is set): "strict" refuses the SSH
                  session; "tofu" trusts the key presented on the first session, records it
                  in status.sshHostKey and enforces it from then on. A known key is always
                  enforced regardless of the policy.
                enum:
                - strict
                - tofu
                type: string
              sshKeySecretRef:
                description: 'SSHKeySecretRef references a Secret containing the SSH
                  private key (key: id_rsa).'
                properties:
                  name:
                    description: name is unique within a namespace to reference a
                      secret resource.
                    type: string
                  namespace:
                    description: namespace defines the space within which the secret
                      name must be unique.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              sshPort:
                default: 22
                description: 'SSHPort is the port sshd listens on inside the remote
                  host (default: 22).'
                type: integer
              sshUserMapping:
                default: inherited
                description: SSHUserMapping controls how the SSH username is determined
                  for callers.
                enum:
                - inherited
                - provided
                - identity
                type: string
            type: object
          status:
            description: LinuxServerStatus defines the observed state of a LinuxServer.
            properties:
              URL:
                description: URL is the proxy URL path for accessing this resource
                  via the hub.
                type: string
              agentVersion:
                description: AgentVersion is the version of the railgrid binary on
                  the agent.
                type: string
              allowedAddons:
                description: |-
                  AllowedAddons are the Addon types the machine owner opted this edge into
                  with the agent's --allow-addon flag, reported on every heartbeat. It is
                  what a portal shows BEFORE anyone creates an Addon: an edge missing the
                  type here will report Allowed=False on any Addon that names it, and
                  nothing will be materialized. Empty (the default) means the edge hosts no
                  add-ons.
                items:
                  type: string
                type: array
              conditions:
                description: Conditions represent the latest observations of state.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              connected:
                description: Connected indicates whether the agent currently has an
                  active tunnel.
                type: boolean
              hostname:
                description: Hostname is the hostname reported by the connected agent.
                type: string
              joinToken:
                description: JoinToken is a bootstrap token for agent registration;
                  cleared on register.
                type: string
              labels:
                additionalProperties:
                  type: string
                description: Labels are propagated from the agent.
                type: object
              lastHeartbeatTime:
                description: LastHeartbeatTime is the most recent agent heartbeat.
                format: date-time
                type: string
              phase:
                description: Phase describes the current lifecycle phase.
                type: string
              sshCredentials:
                description: SSHCredentials holds the SSH auth credentials, set by
                  the agent.
                properties:
                  passwordSecretRef:
                    description: 'PasswordSecretRef references a Secret containing
                      the SSH password (key: "password").'
                    properties:
                      name:
                        description: name is unique within a namespace to reference
                          a secret resource.
                        type: string
                      namespace:
                        description: namespace defines the space within which the
                          secret name must be unique.
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  privateKeySecretRef:
                    description: 'PrivateKeySecretRef references a Secret containing
                      the SSH private key (key: "privateKey").'
                    properties:
                      name:
                        description: name is unique within a namespace to reference
                          a secret resource.
                        type: string
                      namespace:
                        description: namespace defines the space within which the
                          secret name must be unique.
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                  username:
                    description: Username is the SSH username to authenticate as.
                    type: string
                required:
                - username
                type: object
              sshHostKey:
                description: |-
                  SSHHostKey is the SSH host public key reported by the agent (authorized_keys
                  format). Recorded once, on the first report (or on the first "tofu"
                  session), and never replaced automatically: a differing later report sets
                  the SSHHostKeyChanged condition instead. Overridden by spec.sshHostKey.
                type: string
              workspacePath:
                description: WorkspacePath is the kcp workspace path this resource
                  lives in.
                type: string
            required:
            - connected
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
